on:
  workflow_dispatch:
    inputs:
      visitor:
        description: 'Enter your GitHub username'
        required: true
jobs:
  add-manual:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
        id: fetch
      - name: Update README
        run: |
          sed -i "/${{ github.event.inputs.visitor }}/! s|<!-- manual -->|<!-- manual --> ![${{ github.event.inputs.visitor }}]\ ${{ github.event.inputs.visitor }}|" README.md
      - name: Commit & push
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "Manual add: ${{ github.event.inputs.visitor }}"
          file_pattern: README.md
